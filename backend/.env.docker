# Configuração de Banco de Dados - Docker PostgreSQL
# Este arquivo substitui o SQLite pelo PostgreSQL via Docker

# PostgreSQL via Docker
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=id_management
POSTGRES_USER=id_user
POSTGRES_PASSWORD=id_secure_2025

# Database URL para SQLAlchemy
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# File System (mantém igual)
BASE_DIR=C:\Users\luiz\Documents\projetos\id-management\cliente

# API Configuration (mantém igual)
API_HOST=0.0.0.0
API_PORT=8000

# Docker Configuration
DOCKER_COMPOSE_FILE=docker-compose.yml
POSTGRES_CONTAINER_NAME=id_management_postgres
