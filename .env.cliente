# CLIENTE - Configuração para conectar no servidor PostgreSQL
# Copie este arquivo como .env na pasta backend/ dos outros PCs

# ============================================================================
# CONFIGURAÇÃO DE REDE - CONECTA NO SERVIDOR POSTGRESQL
# ============================================================================

# IP do servidor onde o PostgreSQL está rodando
POSTGRES_HOST=192.168.1.52

# Porta do PostgreSQL (padrão: 5432)
POSTGRES_PORT=5432

# Nome do banco de dados
POSTGRES_DB=id_management

# Credenciais de acesso
POSTGRES_USER=id_user
POSTGRES_PASSWORD=id_secure_2025

# ============================================================================
# CONFIGURAÇÕES OPCIONAIS
# ============================================================================

# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Pasta raiz para criação de estruturas de empresas (opcional)
# BASE_FOLDER_PATH=C:\ID_Management\Empresas

# ============================================================================
# INSTRUÇÕES DE USO
# ============================================================================

# 1. Copie este arquivo como .env na pasta backend/
# 2. Atualize o POSTGRES_HOST com o IP correto do servidor
# 3. Execute: uvicorn app.main:app --host 127.0.0.1 --port 8001 --reload
# 4. Acesse: http://localhost:5173 (frontend)

# ============================================================================
# VERIFICAÇÃO
# ============================================================================

# Para testar se está conectando:
# Acesse: http://localhost:8001/database-info
# Deve mostrar: "type": "postgresql"
